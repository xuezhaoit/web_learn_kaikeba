{
  "name": "babel",
  "version": "0.1.1",
  "description": "this is a test project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js --port=80 -s -O a.log",
    "build": "babel js -d build"
  },
  "author": "blue",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
